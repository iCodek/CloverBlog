export const user = state => state.user

export const isLogin = state => state.login

export const song = state => state.user.song

export const blogs = state => state.blogs

export const category = state => state.category

export const tagsOn = state => state.tagsOn

export const blogBox = state => state.blogBox

export const boxWidth = state => state.boxWidth
